import React, { Component } from "react";

export default class Campaign extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return <p>This is the campaign page</p>;
  }
}
